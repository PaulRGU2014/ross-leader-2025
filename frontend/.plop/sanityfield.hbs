import {defineField, defineType} from 'sanity'

export const {{camelCase name}} = defineType({
  name: '{{camelCase name}}',
  title: '{{titleCase name}}',
  type: 'document',
  fields: [
    defineField({
      name: 'title',
      type: 'string',
    }),
    defineField({
      name: 'theme',
      type: 'string',
      options: {
        list: ['light', 'dark'],
        layout: 'radio',
      },
      initialValue: 'light',
    }),
  ],
})