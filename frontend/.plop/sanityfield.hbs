import {defineField, defineType} from 'sanity'

export const {{name}} = defineType({
  name: '{{name}}',
  title: '{{name}}',
  type: 'document',
  fields: [
    defineField({
      name: 'title',
      type: 'string',
    }),
  ],
})